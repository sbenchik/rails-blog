<div class="ui center aligned text container" id="articles" style="display: block;">
  <% @articles.each do |article| %>
    <div class="ui vertical segment">
      <%= link_to article.title, article_path(article)%>
    </div>
  <% end %>
<br>
    <%= link_to "Create a New Article", new_article_path, class: "ui huge download button" %>
</div>